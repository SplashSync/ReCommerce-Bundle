
################################################################################
## Test Configuration for Splash Bundle
splash:
    connections:

        optilog:
            id:                 ThisIsOptilogWsId
            key:                ThisIsNotASecretKeyForOptilog
            name:               Serveur 1 (Optilog Prepod)
            host:               "%splash_host%/ws/soap"
            server_host:        "%splash_host%"
            connector:          optilog
            config:  
                WsHost:         https://api.preprod.geolie.net/wsgestinbox.asmx
                ApiUser:        splash
                ApiPwd:         ThisIsSecretApiPwd
                ApiKey:         ThisIsSecretApiKey
                ApiOp:          ThisIsSecretApiOp
                dfStock:        Divers
                dfGamme:        Nouveaux
